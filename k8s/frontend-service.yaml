apiVersion: v1
kind: Service
metadata:
  name: quickpad-frontend
  labels:
    app: quickpad-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: quickpad-frontend

